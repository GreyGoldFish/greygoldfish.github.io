{% if sidebar %}
<aside>
    {# Recent Posts Section #}
    {% if sidebar.recentPosts and sidebar.recentPosts.listing %}
    <section>
        {% set listing = sidebar.recentPosts.listing | merge({ "reverse": true }) %}
        
        {% include partials.listing %}
    </section>
    {% endif %}
</aside>
{% endif %}
