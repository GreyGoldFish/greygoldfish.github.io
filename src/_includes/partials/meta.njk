{% set pageTitle = title + ' - ' + siteData.name %}

{# We don't want any duplication. This is likely for the home page. #}
{% if siteData.name === title %} 
  {% set pageTitle = title %}
{% endif %}

{% set siteTitle = siteData.name %}
{% set currentUrl = siteData.url + page.url %}

{# If the page’s Front Matter has specific metaTitle and/or metaDesc items, switch them into the mix. #}
{% if metaTitle %}
  {% set pageTitle = metaTitle %}
{% endif %}

{% if not metaDesc %}
  {% set metaDesc = summary %}
{% endif %}

<title>{{ pageTitle }}</title>

{% if siteData.authorName %}
<meta name="author" content="{{ siteData.authorName }}">
{% endif %}

{% if metaDesc %}
<meta name="description" content="{{ metaDesc }}" />
<meta name="twitter:description" content="{{ metaDesc }}" />
<meta property="og:description" content="{{ metaDesc }}" />
{% endif %}

<meta name="theme-color" content="#333333">

<link rel="canonical" href="{{ currentUrl }}" />

<link rel="icon" href="/assets/favicon.ico" sizes="any">
<link rel="icon" href="/assets/icon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/assets/apple-touch-icon.png">

<meta property="og:site_name" content="{{ siteTitle }}" />
<meta property="og:title" content="{{ pageTitle }}" />
<meta property="og:type" content="website" />
<meta property="og:url" content="{{ currentUrl }}" />

{% if socialImage %}
<meta name="twitter:card" content="summary_large_image" />
<meta property="og:image" content="{{ socialImage }}" />
<meta name="twitter:image" content="{{ socialImage }}" />
<meta property="og:image:alt" content="Page image for {{ siteData.name }}" />
<meta name="twitter:image:alt" content="Page image for {{ siteData.name }}" />
{% endif %}

{# --- Font Preconnects and Stylesheets --- #}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Lora:wght@400;700&display=swap" rel="stylesheet">

{# --- Main Stylesheet --- #}
<link rel="stylesheet" href="{{ paths.tailwind }}">
